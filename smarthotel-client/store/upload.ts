import Product from '../interfaces/Product'
export const state = (): object => {
  return {
    response: {},
  }
}
export const mutations = {
  uploadProduct(state: any, product: Product) {},
}
export const actions = {
  uploadProduct(store: any, product: Product) {},
}
